
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE
    username: sa
    password:
  jpa:
    properties:
      '[jakarta.persistence.schema-generation.database.action]': drop-and-create # possible values of: none, create, drop, drop-and-create
